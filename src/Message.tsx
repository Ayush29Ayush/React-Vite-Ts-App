//! PascalCasing - a general convention where we keep the 1st char of function name as a capital letter

function Message() {
  // JSX: JavaScript XML -> Converts the HTML code to JS code
  // We can also dynamically pass something which returns something
  const name = "Ayush Senapati";
  return <h1>Hello {name}</h1>;
}

export default Message;
